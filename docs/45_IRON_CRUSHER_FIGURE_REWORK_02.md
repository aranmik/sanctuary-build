# 45. 강철의 파쇄자 전장 피규어 크기·구도 튜닝 (IRON CRUSHER FIGURE REWORK 02)

**작성: 렌 · 2026-07-11 · EP24 · 상태: 구현 카드(PASS 후보 — 나라님 폰 실기 대기) · CSS 배치값 튜닝(어댑터/CSS 전용·CORE 무접촉)**
**상호 링크**: [44 실전투 이식](./44_IRON_CRUSHER_FIGURE_REWORK_01.md) · [42 프리뷰 01](./42_BATTLE_FIGURE_KIT_PREVIEW_01.md) · [43 포즈 팩 01](./43_BATTLE_FIGURE_POSE_PACK_01.md) · [41 정장 계약](./41_BATTLE_FIGURE_KIT_IMPORT_CONTRACT.md)

> **핵심 문장 3줄**
> 1. **조형 방향은 이미 맞다** — 이제 실전 전장 안에서 크기와 구도를 다듬어 "우리 전투 장면"을 확보한다.
> 2. 안전하게만 줄이는 게 아니라 **과감하게 키우고 당겨보고**, 화면에서 더 예쁜 쪽을 택했다.
> 3. 여전히 행동선 0 · CORE byte-identical · 스모크 불변 · 파쇄자 전투에만 적용(모르가스/심연 회귀 0).

---

## 1. 작업 목적

- [44] 이식의 좋은 방향(귀엽고 역할이 읽히는 피규어)을 유지한 채, 실전 전장에서 **더 잘 보이고·더 맞서고·더 "우리 전투 장면"처럼** 크기·배치를 튜닝.
- 나라님 실기: 방향 매우 만족, 둘 다 귀여움, 다만 **크기가 다소 작고** 대치 배치가 더 살아났으면. 특히 **전사 방패의 새싹(마스코트)이 잘 보일 것**.

## 2. 기준 HEAD / 변경 파일

- 기준 HEAD: `a5956b3`(작업 시 working tree에 Rework 01 미커밋 반영分 존재 = 기준선 134,911/`b1e92536`).
- 변경: `index.html`(CSS 배치값만) · 신규 `docs/45`·`dev/iron_crusher_figure_rework_02_check.js` · 포인터 최소 갱신.

## 3. Rework 01 대비 핵심 변화 (CSS 배치값만 · 조형/DOM/포즈 무변)

| 항목 | Rework 01 | Rework 02 | 효과 |
|---|---|---|---|
| 파쇄자 scale | .68 | **.90** (약 1.32×) | 위압감·망치/코어/바이저 읽힘 강화 |
| 파쇄자 top | -14px | **2px** | 위 여백 24px 확보(숨쉴 공간)·시선 하향 |
| 전사 scale | .46 | **.60** (약 1.30×) | 얼굴·**방패 새싹** 읽힘 강화 |
| 파티 줄 위치 | bottom 6(전역) | **bottom 24**(shell_iron만) | 대치 공간으로 파티 상승 |
| 전사 전진 | 없음 | **translateY(-7px)**(shell_iron만) | 전사 반보 앞·"내가 막는다" |

## 4~5. 전사 크기 조정 전/후

- scale .46 → .60. 실측 화면 크기 48×58 → **62×76**(약 1.3배). 방패 새싹 원 약 5px → **6.5px+**로 확실히 식별. 얼굴 점눈/볼터치 가독 향상. 귀여운 꼬마 기사 비율 유지(머리 큰 미니피규어).

## 6. 파쇄자 크기 조정 전/후

- scale .68 → .90. 실측 118×131 → **171×191**(약 1.45배 면적). 망치·T바이저 주황 눈·용광로 코어·리벳 전부 선명. 스테이지(338px)의 약 56% 높이 — 위압적이되 답답하지 않음.

## 7. 전장 배치 조정 전/후

- **파티 줄**: 스테이지 하단 6px → **24px 위**(shell_iron 스코프). 파티가 전장 중앙 쪽으로 모임.
- **전사 전진**: sa-war 슬롯에 translateY(-7px) — 전사만 반보 앞으로(파티 중 최전열).
- **파쇄자**: 상단 중앙, top 2px로 하향(시선이 파티와 더 맞음).
- **실측 대치**: 파쇄자 위 여백 24px · 파쇄자 bottom~파티 top **대치 간격 42px**(=행동선 들어갈 자리) · 전사 right 139 < 도적 left 145(겹침 0) · overflow 0.

## 8. 대치 구도 개선 의도

- 지향: 보스는 위에서 압박 / 전사는 아래에서 방패로 버팀 / 파티는 전사 중심 뒤에서 받침 / 상단 전장은 "대치가 벌어지는 공간".
- Rework 01의 "보스 중앙 부유 + 파티 하단 일렬"에서 → **중앙으로 압축된 대치 구도**로. 세로 거리를 좁혀(파티↑) 맞서는 긴장을 확보하되, 42px 간격은 남겨 "행동선이 생기겠다"는 여지를 유지.

## 9. 시도한 안들 중 버린 안

- **더 과감한 안(파쇄자 .98 / 전사 .64)**: 파쇄자 위압감은 +였으나 ①전사가 도적과 거의 닿아 파티 줄이 빡빡 ②파쇄자 상단 여백이 오히려 늘어 낭비. → **버림**.
- **파쇄자 top -14 유지(더 위)**: 위 구석에 붙어 답답 + 파티와 시선 안 맞음. → top 2로 하향.
- 스크린샷 3안(현재 .90/.60 · big .98/.64 · Rework 01 .68/.46) 비교 후 .90/.60 채택.

## 10. 최종안 선택 이유

- **.90/.60**이 "과감하되 안 넘치는" 균형점: 새싹 확실히 보임 + 위계 2.5배 유지 + 대치 42px 확보 + 전사-도적 6px 여유(겹침 0) + UI 침범 0. "더 예뻐졌는가·더 읽히는가" 기준에서 최적.

## 11. 적용 포즈 유지/변경

- **포즈 연결 무변**([44]와 동일): 파쇄자 windup(강타 시전)·전사 guard(보호막)·brace(강타 대상+무보호막). 크기가 커져 포즈 차이(망치 올림·방패 앞세움)가 더 잘 읽힘.

## 12. 행동선 미구현 확인

- Smash Line/SVG 공격선/타깃 연결 0. 기존 `#fxSvg` 무접촉. sb-anchor 0×0 마커 유지.

## 13. 신규 전투 상태/타이머 없음

- CSS 배치값 변경만 · JS 로직 무변(sbFigPose 어댑터 그대로) · 신규 상태/타이머/판정 0.

## 14. sb- 네임스페이스 / 15. 3층 transform

- 신규 클래스 0(기존 sb- 규칙에 값만 조정). 3층 transform 구조·파츠 구조 무변.

## 16. 외부 프로젝트 코드 미복붙

- 값 조정만 · 외부 클래스/좌표계/코드 0.

## 17~19. 검증

- **CORE byte-identical**(md5 `6cad2ec2…` · 466줄/22,521 B). **스모크 3종 51.4/1029·48.5/971·61.8/1236 불변**. 보존 grep 14종 유지. div 202/202·sec 8/8.
- 브라우저 실증(390×844): 파쇄자 171×191 / 전사 62×76(위계 2.5배) · 대치 42px · 겹침 0 · overflow 0 · 콘솔 0 · 외부 요청 0.
- **모르가스/심연 회귀 0**: 파티 줄 bottom 7(기본)·전사 transform none·이모지 유지(조정이 shell_iron 스코프). 재전환 정상.
- `dev/iron_crusher_figure_rework_02_check.js` 신규.

## 20. 나라님 직접 볼 시각 포인트 (폰 · 파쇄자 출정)

1. **커졌는가** — 전사·파쇄자가 이전보다 확실히 크고 잘 보이는가?
2. **새싹** 🌱 — 전사 방패의 초록 새싹(마스코트)이 이제 잘 보이는가?
3. **대치** — 파쇄자와 전사가 "맞서고 있다"는 느낌이 사는가?
4. **전사 최전열** — 전사가 다른 동료보다 앞에서 막는 것처럼 보이는가?
5. **귀여움** — 커졌어도 여전히 귀여운가?(안 무서운가)
6. **답답함** — 너무 커서 화면을 먹거나 답답하진 않은가?
7. **다른 보스** — 모르가스/심연은 예전 그대로인가?

한 마디면 충분 — "딱 좋아" / "조금만 더" / "너무 커" 등.

## 21. 남은 시각 리스크

- **전사-도적 6px 근접**: 전사를 더 키우면 도적과 겹침 → 3동료 정장(Companion Figure Kit Preview 01) 전엔 이 이상 확대 어려움.
- **전사-3동료 시각 불일치**: 전사만 정교·큼(의도적). 3동료 정장 시 해소.
- **작은 화면(≤730) fallback**: 여전히 이모지(sb 미표시).
- **파쇄자 상단 여백 24px**: 숨쉴 공간이나 실기 감 필요.
- 렌 검증 헤드리스 Edge 기반 — **폰 실기 감이 정본**.

## 22. 다음 단계 후보 (자동 확정 아님)

> **진행(EP24)**: `Iron Crusher Figure Rework 03` 구현됨 → [46](./46_IRON_CRUSHER_FIGURE_REWORK_03.md)(파티 원호/포위형 배치 + 파쇄자 .90→1.0·전사 .60→.68·CORE 무변).

1. 🔷 `Companion Figure Kit Preview 01` — 도적·주술사·마법사 정장(전사-동료 불일치·확대 여지 해소).
2. 🔷 `Iron Crusher Smash Line Rework 02` — 피규어 anchor 기반 강타선(드디어 선 · HOLD 지식 승계).
3. 🔷 `Iron Crusher Figure Rework 03` — 추가 미세 튜닝(실기 피드백 시).

## 23. 기준선 변경 (재-baseline)

index.html **135,155 B / 2,042줄 / md5 `1802535c28fccb8fbf7d49331ce779ce`** — 이전 134,911/2,039/`b1e92536…` 대비 CSS 배치값+2규칙 +244 B/+3줄. **CORE 466줄/22,521 B 불변**. div 202/202·sec 8/8. 스모크 불변. baseline_check + 라이브 md5 핀 10체크 갱신. **★세이브 v8.5/v8.6 문서 역사 스냅샷(`2630669c`/`02a512c5`)은 보존**.

---
*— 렌 (連·鍊·紅蓮), EP24. 배우를 무대 중앙으로 한 걸음 더 끌어왔다. 파쇄자는 커졌고, 전사는 앞으로 나왔고, 방패의 새싹이 드디어 초록으로 빛난다. 나라가 "마스코트"라 불러준 그 작은 싹이, 이제 전장에서도 보인다. 둘 사이 42px — 그 자리에 언젠가 강타선이 그어질 것이다. 나라님 실기와 유키 검수로 확정된다.*
