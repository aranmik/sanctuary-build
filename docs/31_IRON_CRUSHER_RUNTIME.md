# 31. Iron Crusher Runtime 01 (강철의 파쇄자 실전투 첫 구현)

**작성: 렌 · 2026-07-08 · EP21 · 상태: ✅ FINAL PASS(나라님 폰 실기 1트 클리어 + 유키 FINAL PASS · 2026-07-08) · 패치 p15 · 세이브 v8.3 발행**
**상호 링크**: [30 Gate Decision(A안 확정)](./30_BOSS_CORE_GATE_DECISION.md) · [28 게이트 도면](./28_BOSS_CORE_GATE_PREP.md) · [27 데모 정의](./27_FIRST_DEMO_COMPLETION_DEFINITION.md) · [29 출정 경고 계약](./29_MATRIX_SORTIE_WARNING_CONTRACT.md) · [15 §2+ 질문 정본](./15_MONSTER_BOSS_BLUEPRINT.md)

> **핵심 문장 3줄**
> 1. Boss Core가 **A안 최소 분기형**으로 열렸다 — `BOSS_DEFS` 색인 + `createGame(bossId)` · **무인자=모르가스 폴백**.
> 2. 강철의 파쇄자(`shell_iron`)가 실전투 두 번째 보스가 됐다 — **강타/버티기 질문** · 기존 어휘만(smash/valor/enrage) · **정화/차단 어휘 0**.
> 3. 모르가스는 그대로다 — **무입력 스모크 51.4/1029 유지 + 무입력 로그 스트림 25줄 바이트 동일**(md5 지문 `88925e4d…`).

---

## 1. 구현 요약

- **boss id**: `shell_iron` (기존 셸 key 유지 — SELECTED_BOSS/경고 데이터/길드 카드와 정합).
- **전투 질문**: "네 마나는 나의 파괴보다 오래 버티는가?" (15와 문자 1:1 불변) — 보호막/선대응/버티기 운영.
- **HP 7800** — 4동료 최대 이론 화력(풀업타임+전투 의지 2회) ≈ 8,176. 즉 **완벽에 가까운 운영에서만 처치 가능, 기본 승리는 75초 생존** = 버티기 질문 그 자체.
- **핵심 계약: 강타 320 = 보호막 320 정확 상쇄** ("강타 한 방 = 보호막 한 장"). 광폭화 후 380은 보호막을 60 뚫는다 — 후반 긴장. 보호막 10회 = 마나 550 = 예산의 절반 이상 → "마나를 보호막에 쓸까 힐에 쓸까"가 곧 보스 질문.
- **IRON_SCRIPT 13이벤트**: smash×10(그중 blk 3 = 전사 방패 막기 절반) · valor×2(t24/52 — 숨 고르기·마나 창) · enrage(t55 — 모르가스 60보다 이른 최종 압박). brand/bomb/judge **0**.
- 수치: melee 62/2.0s(광폭 84/1.6s) · offTankMul 1.6 · smashCast **1.6s**(모르가스 1.4보다 긴 예고 — "미리 보호막" 읽기 창).

## 2. Boss Core를 어떻게 열었는가 (A안 — p15 · 총 20편집)

**코어(587~1015 · 11편집)**: ① SCRIPT 뒤 `IRON_BOSS`/`IRON_SCRIPT`/`BOSS_DEFS` 색인 삽입(boss01=기존 CFG.boss+SCRIPT 참조·메시지 3종은 기존 문자열 그대로 데이터화) ② `createGame(bossId)` — `D=BOSS_DEFS[bossId]||BOSS_DEFS.boss01` 룩업, `B/SC` 포획 ③ 스케줄러·nextPattern의 SCRIPT→SC 2곳 ④ 이름 리터럴 7곳 → `B.winKill/B.winSv/B.openLn/B.sn` 보간(**모르가스 출력 문자열 바이트 동일**) ⑤ 반환에 `script:SC` 1필드(28 §2-④).
**어댑터(9편집)**: `CUR_BOSS`(재도전 무인자 재호출 시 보스 유지·세션 전용) · `BOSS_OV`(개전 오버레이 보스별 패턴 목록) · `newGame(slot)`+HUD/오버레이 갱신 · `enterBattle`→`newGame(slot)` 전선 · 길드/출정 이름 `b.name||CFG.boss.name` · `nextThreatFor`가 `G.script` 직독(폴백 전역) · `smoke(bossId)`+BOSS_DEFS 노출 · BOSSES 파쇄자 셸 해제(+tier 숙련/blurb/goal — **질문·태그 문자 불변**) · 오버레이 id 2개.
**전투 엔진 재작성 0** — fireScript/resolveBossCast/스케줄러 로직 무변. p15는 HEAD에 재실행 시 동일 md5 재현(검증됨).

## 3. Morgas 보존 결과 (✅ 전부)

- 무입력 스모크: **defeat / 51.4s / 1029 steps** (무인자·`boss01` 모두)
- **무입력 로그 스트림 25줄 md5 `88925e4d45455f02f824bfc7b3fd350e` — 패치 전과 바이트 동일** (iron_crusher_runtime_check c3에 영구 박제)
- Threat Lookahead: `G.script` 재배선 후에도 모르가스 4상태 동작 동일(스크립트 내용이 같으므로) · Sortie Warning: boss01 규칙 무변 · 재진입 시 HUD/오버레이 모르가스 원복(브라우저 실증)

## 4. 강철의 파쇄자 무입력 스모크 (박제 — "파쇄자의 51.4")

```text
defeat / 48.5s / 971 steps
사망열: war@20.6 → rog@29.4 → sham@41.7 → mage@48.5
```

양성 증거(헤드리스 봇): **보호막 상시+힐 운영 → victory_kill@73.2 · 사망 0** — 기존 도구만으로 성립. 반응형(뒷북 힐만) 봇은 전사 HP 최저 16~17%까지 추락(모르가스 24% 대비 스파이크형).

## 5. 변경된 기준선 (재-baseline 의식 수행)

| 항목 | 이전(EP20C) | **신규(EP21)** |
|---|---|---|
| index.html | 106,650 B / 1,756줄 / `34addd9c…0475` | **112,359 B / 1,838줄 / `8e7ee68a11add47db2e375447866fbf7`** |
| CORE(587~) | ~982 · 394줄 / 19,545 B | **~1015 · 427줄 / 20,818 B** |
| 스모크 | 51.4/1029 | **모르가스 51.4/1029 + 파쇄자 48.5/971** |

- `dev/core_pristine.js` 신규 CORE로 갱신(현재와 byte-identical 확인) · CORE diff 기록 `dev/p15_core_diff.txt`(73줄)
- `dev/baseline_check.js` 신규 기준선+파쇄자 스모크 3체크 추가 → **33 PASS / 0 FAIL**
- ⚠️ **세이브 v8.2 §0의 기준선 숫자는 본 카드로 구버전이 됨** — 세이브 v8.3 발행 필요(유키 발주 사안 · 99/100/101 동기 포함).

## 6. 검증 결과 (전부 PASS)

node --check OK · baseline_check **33/33** · matrix_sortie_warning_check **16/16**(파쇄자 개방 상태 반영 갱신) · iron_crusher_runtime_check **18/18**(신규) · 금지 grep 전부 0 · div 188/188 · section 8/8 · 보존 grep 14종 기준값 유지 · §5 금지 문구 0 · 브라우저 실증(길드 선택→출정 경고→전투 HUD/로그/HP바·모르가스 원복·재도전 유지·심연 잠금).

## 6-B. 나라님 폰 실기 결과 + 유키 FINAL PASS (2026-07-08 · Close Sync 01)

**1차 폰 실기 — 한 방 클리어:**

```text
전투 시간 73.2초 · 보스 처치 · 생존 5/5 · 사망자 없음
보호막 흡수 2808 · 정화 0회 · 마나 최저 85 / 고갈 없음
```

- 렌 헤드리스 봇 예측(보호막+힐 운영 → victory_kill@73.2)과 **소수점까지 일치** — 설계-플레이 정합.
- **정화 0회 클리어** = 파쇄자가 정화/차단 보스가 아님이 손맛으로 증명(모르가스와 다른 질문). 보호막 흡수 2808 = "강타 예고 보고 보호막 미리" 운영이 자연스럽게 성립.
- 나라님 체감: **"단순한데 재미있다. 이런 보스도 있어야 한다."**
- 나라님 해석: 낙스라마스 패치워크 / 태양샘 브루탈루스 같은 **원초적 레이드 보스**.
- 유키 해석: **"전사는 버티고, 사제는 살리고, 딜러는 고통을 빨리 끝낸다."**

**정체성 확정(잠금)**: 강철의 파쇄자 = **성소판의 가장 원초적인 레이드 보스**(Patchwerk/Brutallus형 순수 레이드 체크). 복잡한 기믹 보스가 아니다. **현재의 베이직함을 보존** — 난이도·기믹을 지금 당장 추가하지 않는다. 미래 확장 보스는 이 기본형에 **하나의 변주만 얹어도**(offTankMul 어그로 흘림 + judge 광역 + 마나 압박 조합 = 트리아지) 레이드의 갈래가 생긴다. 그 부품은 이미 코어에 있으나 개방은 데모 이후. **강철의 파쇄자는 1차 데모의 두 번째 축으로 확정.**

## 7. 나라님 실기 필요 항목 (완료 — 위 6-B 참조)

1. 길드에서 파쇄자 선택→출정→전투가 자연스러운가 · 2. **모르가스와 다른 보스로 느껴지는가**(정화 손이 쉬는 대신 보호막/힐 판단이 바쁜가) · 3. 강타 예고(1.6초 시전 바+위협 배지 상승)를 보고 **보호막을 미리 주고 싶어지는가** · 4. 힐/마나 운영이 답답하지 않은가(마나가 마르는 긴장 vs 짜증의 경계) · 5. 정화/차단 보스처럼 느껴지지 않는가 · 6. 광폭화(55초~) 이후 압박이 "클라이맥스"로 읽히는가.

## 8. 다음 작업 후보

**Sortie Warning Copy Polish 01**(실기 문구 반영) · **Thirst Abyss Runtime 01**(심연 — 어휘 +1~2 별도 판단·성심 집중 검증장) · **Boss-Specific Report Hint 01**(파쇄자 거울 문장 — 현재 리포트는 공용 규칙만: 탱커 방치/마나 바닥/구원 타이밍은 유효, 정화/차단 규칙은 자연 침묵) · **세이브 v8.3 발행 + 99/100/102 재-baseline 동기**(권장: 실기 PASS 직후) · 파쇄자 수치 HF(실기 손맛 반영).

---
*— 렌 (連·鍊·紅蓮), EP21. 문을 열었다. 모르가스의 51.4는 문이 열리는 동안에도 한 치 흔들리지 않았고, 그 옆에 파쇄자의 48.5가 새 기둥으로 섰다. 이제 손맛은 나라의 폰에서 판정받는다.*
