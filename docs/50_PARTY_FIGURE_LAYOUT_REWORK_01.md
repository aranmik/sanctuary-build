# 50. 파티 전장 배치 재조정 (PARTY FIGURE LAYOUT REWORK 01 — 안 B: 과감한 포위 확대)

**작성: 렌 · 2026-07-12 · EP24 · 상태: 실전 구현(shell_iron 전투) · 기준 HEAD `5e59575` · 계약: [41](./41_BATTLE_FIGURE_KIT_IMPORT_CONTRACT.md)**
**상호 링크**: [49 파티 실전 이식 01](./49_COMPANION_FIGURE_PARTY_REWORK_01.md) · [48 동료 색·재질 02](./48_COMPANION_FIGURE_KIT_PREVIEW_02.md) · [39 시각 정체성 계약](./39_BATTLE_VISUAL_IDENTITY_CONTRACT.md)

> **핵심 문장 3줄**
> 1. **전장 배치만 봐도 "어? 재미있어 보인다"가 먼저 와야 한다.**
> 2. 아군이 오밀조밀 조잡하게 모인 게 아니라, **하나의 군집으로서 큰 보스와 대등하게 맞선다**는 인상.
> 3. 행동선 때문에 캐릭터를 작게 두는 단계가 아니다 — **먼저 파티가 전장을 장악**하게 만든다.

---

## 1. 작업 목적

- Party Rework 01([49])은 "4인이 실제 전장에 섰다"는 점에서 성공했으나, 나라님은 (a) 파티가 아직 오밀조밀하고 (b) 전사만 상대적으로 크고 나머지 셋은 조심스럽게 들어갔으며 (c) 전장을 더 꽉 차게 쓰고 싶고 (d) "큰 보스를 포위하며 대등하게 맞선다"는 감각을 더 강하게 원했다.
- 이번 카드 목표: **4인 파티를 더 크게·더 벌려·더 당당하게** 만들어, 강철의 파쇄자를 포위하며 맞서는 전장 장면을 완성한다. 신규 조형·포즈·행동선은 없고, **크기/간격/원호 배치 값만** 공격적으로 재조정한다.

## 2. 기준 HEAD / 시작 working tree 상태

- 기준 HEAD: `5e59575`. 시작 시 **Party Rework 01 미커밋 결과** 위에서 이어 작업(reset/restore/clean/checkout 하지 않음).
- 시작 index.html: **149,309 B / 2,097줄 / md5 `c9e289d7…` · CORE 466줄/22,521 B/md5 `6cad2ec2…`**.
- 시작 스모크: 모르가스 51.4/1029 · 파쇄자 48.5/971 · 심연 61.8/1236.

## 3. 변경 파일 (이번 카드)

- `index.html` — 4인 scale/gap/원호 transform 재조정(안 B). CSS/어댑터 층만.
- `dev/p23.mjs` — 재현 패치(10 edits). 신규.
- `docs/50_PARTY_FIGURE_LAYOUT_REWORK_01.md` — 본 문서. 신규.
- `dev/party_figure_layout_rework_01_check.js` — 전용 검증. 신규.
- `dev/baseline_check.js` + 라이브 md5 핀 14종 — 재-baseline(c9e289d7→bb7fc147).
- `docs/49·98·99·102` — 포인터 갱신.

## 4. 나라님 피드백 정본

1. 전투 룩이 가장 중요하다. 2. 배치만 봐도 "어? 재미있어 보인다"가 와야 한다. 3. 조잡하게 모인 느낌 회피. 4. 파티 군집이 큰 보스와 대등하게 맞서는 장면. 5. 캐릭터를 더 키우고 싶다. 6. 간격을 더 주고 싶다. 7. 포위 형태를 더 강하게. 8. 행동선 때문에 공간을 아끼는 단계는 아직 아니다. 9. 지금은 "우리의 전투 장면"을 먼저 확보한다.

## 5. 왜 이 카드가 필요한가 — 실측으로 확인된 여유 공간

- pane(375 뷰포트) 실측: 스테이지 내부 폭 **359px**(left 8 ~ right 367), 파쇄자 중심 ≈188.
- Party Rework 01 4인 중심 x: 전사 111 / 도적 168 / 마법사 213 / 주술사 267 → **스팬 156px만 사용**, 좌우로 **각 ~65px씩 여백이 남아 있었다**. 나라님이 느낀 "오밀조밀"이 수치로 확인됨.
- 즉 더 키우고 더 벌릴 물리적 여유가 충분했다. 이번 카드는 그 여유를 쓴다.

## 6. Party Rework 01에서의 한계

- 전사만 .74, 나머지 .66~.68로 실전 안정성을 우선한 보수적 크기.
- 원호 transform이 안쪽으로 모여(전사 +4로 오른쪽, 도적 +7로 오른쪽) 네 중심점이 중앙에 뭉침 → 양쪽 65px 낭비 + 도적/마법사가 보스 바로 아래 밀집.

## 7~11. 시도한 배치안 (A / B / C) — 크기·간격·충돌·장단점

pane 런타임 실측(375 뷰포트, 값 오버라이드 후 getBoundingClientRect)으로 3안을 비교했다.

| 안 | 전사 | 도적 | 마법사 | 주술사 | 파쇄자 | gap | war/rog/mage/sham translate | 실측 스팬 | 좌·우 여백 | 인접 gap | 판정 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **A 균형확대** | .78 | .72 | .74 | .72 | 1.02 | 22 | (-16,-14)(-4,6)(4,6)(16,-8) | 224 | 27 / 30 | [-2,-4,0] | 안전하나 rog-mage 살짝 겹침·소심 |
| **B 과감포위** | **.80** | **.74** | **.76** | **.74** | **1.04** | **26** | **(-24,-12)(-8,8)(8,8)(24,-6)** | **252** | **12 / 15** | **[4,6,6]** | **★채택** |
| C 간격최대 | .76 | .71 | .73 | .72 | 1.0 | 34 | (-20,-13)(-2,7)(2,7)(20,-7) | 268 | 6 / 8 | [18,6,19] | 여백 6/8 과타이트·크기 부족 |

- **각 안 충돌 여부**: A는 인접 bounding gap이 [-2,-4,0]로 도적-마법사가 겹치는 경향. B는 [4,6,6] 전부 양수(안 겹침). C는 바깥 넓으나 rog-mage는 6으로 여전, 좌우 여백 6/8로 스테이지 끝에 붙음.
- **각 안 장단점**: A=안전하지만 크기·스팬이 B보다 소심해 "당당함" 부족. B=크기 최대 + 넓은 날개 + 전부 양수 gap + overflow 0, 파쇄자도 1.04로 함께 커져 대등감 최강. C=좌우로 가장 넓지만 크기(파쇄자 1.0 포함)가 작아 전장 점유감·존재감이 오히려 약하고 여백이 위험.

## 12. 버린 안과 이유

- **안 A 버림**: rog-mage 겹침(-4) + 크기가 B의 하위호환(전 항목이 B보다 작음). B가 상위호환.
- **안 C 버림**: 좌우 스팬은 최대(268)지만 파쇄자 1.0·4인 크기 축소로 "더 크게" 요구를 정면으로 배신. 좌우 여백 6/8은 실기 폰 편차에서 잘림 위험.

## 13. 최종 선택안 (안 B) — 최종 scale/translate 값

```
#sb-boss-fig  scale 1.04            (파쇄자 소폭 확대)
#sb-war-fig   scale .80  z-index 4
#sb-rog-fig   scale .74  z-index 3
#sb-mage-fig  scale .76  z-index 3
#sb-sham-fig  scale .74  z-index 3
#stage.sb-boss-iron #stageAllies   { bottom:26px; gap:26px }
#stage.sb-boss-iron #sa-war   translate(-24px,-12px)  z-index 4   (좌전방 날개)
#stage.sb-boss-iron #sa-rog   translate( -8px,  8px)             (좌중, 앞아래)
#stage.sb-boss-iron #sa-mage  translate(  8px,  8px)             (우중, 앞아래)
#stage.sb-boss-iron #sa-sham  translate( 24px, -6px)             (우외곽 날개)
```

- 최종 실측(pane 375): 4인 중심 x = 전사 62 / 도적 146 / 마법사 229 / 주술사 314, **스팬 252**, 좌 여백 12·우 여백 15, 인접 gap [4,6,6] 전부 양수, 스테이지 overflow 0.

## 14. 전사/도적/마법사/주술사/파쇄자 체감 위계

- **전사 .80**(최전열 보호자·좌측 최전방·z4·방패) > **마법사 .76**(후열 주딜) ≈ **도적 .74** ≈ **주술사 .74**(지원). 전사는 여전히 가장 크고 앞에 있으나, 나머지 셋이 .66~.68 → .74~.76으로 올라와 "전사만 혼자 큰" 느낌이 크게 줄었다. **파쇄자 1.04**로 보스도 함께 커져 파티와 대등한 무게.

## 15. 전장 점유감 결과

- 좌우 스팬 156 → **252px**(스테이지 폭 359의 70%). 전사 날개(x-24)와 주술사 날개(x+24)가 파쇄자 몸통 폭(x 89~286) **바깥으로 뻗어** 얕은 포위(∪형: 바깥 날개 위, 안쪽 둘 앞아래)로 읽힌다. 헤드리스 컷에서 4인이 파쇄자를 넓게 감싸며 대치.

## 16. renderFx 보존 방법

- 배치는 `#sa-*` 컨테이너의 transform과 `#sb-*-fig`의 scale만 조정. 각 슬롯의 `fxr-*`(링)·`fxf-*`(플래시) 스팬은 그대로 유지 → renderFx 경로 무접촉. sb는 여전히 `figAura/figBody`(원형 피규어)만 숨긴다.

## 17. `sb-` 네임스페이스 확인

- 신규 클래스 0. 기존 `sb-*` 요소의 scale/transform 값만 변경. 일반명(.body/.orb/.totem 등) 0.

## 18. 3층 transform 유지 확인

- `#stage .sb-unit`(배치) / `.sb-react`(포즈) / `.sb-fit`(반전·스케일) / `.sb-fig`(호흡) 구조 유지. 이번 변경은 배치층(#sa-*, #sb-*-fig)만.

## 19. 행동선 미구현 확인

- SVG/stroke/폴리라인/강타선 0(`sb-line`·`sbSmashLine`·`<svg>` 부재). 중앙 전투축(파쇄자 하단 ~ 안쪽 두 동료 사이)에 미래 행동선용 최소 통로는 남겨둠(안쪽 둘을 앞아래 y+8로만 이동, 중앙 상하 라인은 비움).

## 20. CORE·수치·판정 무변경 확인

- CORE 마커 추출 **466줄/22,521 B/md5 `6cad2ec2…` byte-identical**(시작과 동일). 강타 `smash:320, enSmash:380, smashBlocked:0.5` 원문 보존. `localStorage.setItem` 1회(Save 확장 0).

## 21. 다른 두 보스 회귀 없음

- sb 배치는 `#stage.sb-boss-iron` 스코프 한정. 모르가스/심연은 이모지(bossAvatar)+원형 피규어 유지, 전역 `#stageAllies{bottom:6px}` 불변. 스모크 3종 동일.

## 22. 390×844 실기 결과

- pane(375) 실측: winInner 375 / bodyScrollW 375 / appW 375 / stageW 359 → **overflow 0**, 콘솔 오류 0, 외부 요청 0. sham 우끝 R352 < 스테이지 R367 → 잘림 없음.
- 헤드리스 Edge 컷(A/B/C) 확보. ★pane 스크린샷은 이번 세션 내내 timeout(렌더러 정상, 캡처만 고장) → 헤드리스로 우회. ★헤드리스 `--headless=new`는 meta viewport를 무시해 앱을 데스크톱 폭으로 그려 우측이 잘리는 아티팩트가 있어, 앱 폭을 375로 가두는 wrapper로 전체 컷 확보(pane 실측 appW=375와 동일 결과).

## 23. 나라님이 직접 볼 포인트 (5183 → 파쇄자 전투)

1. 4인이 더 크게 보이는가. 2. 전사만 혼자 큰 느낌이 줄었는가. 3. 전사가 더 왼쪽/전방에서 막는가. 4. 주술사가 더 오른쪽/외곽에서 파티를 지지하는가. 5. 도적/마법사가 너무 붙어 있지 않은가. 6. 보스를 포위하는 느낌이 강해졌는가. 7. 오밀조밀함이 줄었는가. 8. 전투 영역을 더 꽉 차게 쓰는가. 9. 여전히 귀엽고 각 역할이 읽히는가.
   한 마디면 충분 — "훨씬 낫다" / "조금 더 벌려" / "전사 더 왼쪽" 등.

## 24. 남은 시각 리스크

- **좌우 여백 12/15px**: 스테이지 폭 여유가 넉넉하진 않다. 폰 기종 폭 편차가 크면 날개(±24)를 소폭 당길 여지(Party Figure Layout Rework 02).
- **원호 세로감**: 바깥 날개 위·안쪽 앞아래의 얕은 ∪형. 더 뚜렷한 포위(안쪽을 더 앞으로)는 포즈/시선과 함께 다뤄야 자연스럽다.
- **마법사 상대 밝기**: 구체 발광 + .76로 후열 중 가장 눈에 띔(의도적이나 실기 확인).
- 렌 검증은 pane 실측 + 헤드리스 컷 기반 — **폰 실기 감이 정본**.

## 25. 다음 단계 후보 (자동 확정 아님)

- 🔷 `Iron Crusher Smash Line Rework 02` — 이제 확보한 전장 위로 강타선 설계.
- `Companion Figure Pose Pack 01` — 3동료 포즈 어휘. ✅ **구현됨** → [51](./51_COMPANION_FIGURE_POSE_PACK_01.md)(독립 프리뷰·도적/마법사/주술사 각 5포즈+전사 4비교·4인 Actor Ensemble·프리셋 4종·index.html 무접촉·24체크 PASS).
- 🔷 `Additional Boss Figure Layout Rework 01` — 모르가스/심연에도 sb 파티(별도 카드).
- 🔷 `Party Figure Layout Rework 02` — 폰 편차 대응 미세 조정.

---
*— 렌 (連·鍊·紅蓮), EP24. 넷을 세워 봤더니 가운데로 모여 65픽셀씩 양쪽을 비워두고 있었다. 그래서 전사를 왼쪽 끝으로, 주술사를 오른쪽 끝으로 보내고 넷 다 몸집을 키웠다 — 파쇄자도 같이 키워서, 우리가 작아서 피하는 게 아니라 같은 크기로 맞선다는 걸 보이게. 세 안을 다 재보니 넓기만 한 C도, 안전한 A도 아니라, 크고 벌어지고 그래도 안 부딪히는 B가 답이었다. 나라님 실기와 유키 검수로 확정된다.*
